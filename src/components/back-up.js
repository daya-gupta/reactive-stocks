	{/* <div ng-show="latestPriceVolumeData">
							<ul>
								<li>Price: {{latestPriceVolumeData.price}} (
									<span className="bold" ng-class="{positivePriceChange: latestPriceVolumeData.relativePrice > 0, negativePriceChange: latestPriceVolumeData.relativePrice < 0}">
										{{latestPriceVolumeData.relativePrice}}%
									</span>)
								</li>
								<li>Volume: {{latestPriceVolumeData.volume}} (
									<span className="bold" ng-class="{positivePriceChange: latestPriceVolumeData.relativeVolume > 1, negativePriceChange: latestPriceVolumeData.relativeVolume < 1}">
										{{latestPriceVolumeData.relativeVolume}} times
									</span>)
								</li>
								<li>Average Volume: {{latestPriceVolumeData.averageVolume}}</li>
							</ul>
						</div> */}

						{/* <div id="tabular-data">
							No Data Available
							<table className="table table-striped table-bordered"></table>
						</div> */}

						{/* <div className="row notes">
							<div className="col-sm-10">
								{this.showNotes && <textarea className="textarea" value={this.stock.note} placeholder='Add notes' rows='6'>notes</textarea>}
							</div>
							<div className="col-sm-2 row">
								{!this.showNotes && <input className="btn btn-primary col-sm-12" ng-click="showNote()" type="button" value="Show Notes">
								{this.showNotes && <input className="btn btn-primary col-sm-12" ng-click="showNotes=false" type="button" value="Hide Notes">
								{this.showNotes && <input className="btn btn-primary col-sm-12" ng-click="saveNote()" type="button" value="Save">
								{this.showNotes && <input className="btn btn-primary col-sm-12" ng-click="showNotes=false" type="button" value="Cancel">	
							</div>
						</div> */}


						{/* Modal */}
						{/* <div className="modal fade" id="newsModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
							<div className="modal-dialog" role="document">
								<div className="modal-content">
									<div className="modal-header">
										<button type="button" className="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
										<h4 className="modal-title" id="myModalLabel"></h4>
									</div>
									<div className="modal-body">
										<h3>Notification:</h3>
										<table className="table table-bordered" id="notification">
										</table>

										<h3>News:</h3>
										<table className="table table-bordered" id="news">
										</table>
									</div>
									<div className="modal-footer">
										<button type="button" className="btn btn-default" data-dismiss="modal">Done</button>
										<!-- <button type="button" className="btn btn-primary">Save changes</button> -->
									</div>
								</div>
							</div>
						</div> */}